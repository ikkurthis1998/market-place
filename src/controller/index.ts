import { uploadImageController } from "./product/uploadImageController";
import { downloadImageController } from "./product/downloadImageController";
export const controller = {
    uploadImageController,
    downloadImageController
};